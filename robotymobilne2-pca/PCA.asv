function [PC] = PCA(X)

[M,N] = size(X);

srednie = mean(X,2);

X = X - repmat(srednie,1,N);

C = 1/(N-1)*X*X';

[PC,V] = eig(C);

V = diag(V);

size(V)
figure;
plot([1:200],V); hold on;

[~,indeksy] = sort(-1*V);
V=V(indeksy);
PC=PC(:,indeksy);

plot(V)



end